name: Sync cars from GDrive to R2 and generate md

on:
  workflow_dispatch: {}

permissions:
  id-token: write
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest

    env:
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_BUCKET: ${{ secrets.R2_BUCKET }}
      R2_PUBLIC_BASE_URL: ${{ secrets.R2_PUBLIC_BASE_URL }}
      GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}

      AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: auto
      AWS_EC2_METADATA_DISABLED: "true"
      AWS_PROFILE: ""
      AWS_CONFIG_FILE=: "/dev/null"
      AWS_SHARED_CREDENTIALS_FILE: "/dev/null"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install python deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install google-api-python-client google-auth google-auth-httplib2 google-auth-oauthlib

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      #- name: Test Drive access (list files)
      #  env:
      #    GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
      #  run: |
      #    python - <<'PY'
      #    from googleapiclient.discovery import build
      #    from google.auth import default
      #    creds, _ = default(scopes=["https://www.googleapis.com/auth/drive.readonly"])
      #    drive = build("drive", "v3", credentials=creds)
      #    folder_id = __import__("os").environ["GDRIVE_FOLDER_ID"]
      #    q = f"'{folder_id}' in parents and trashed = false"
      #    res = drive.files().list(q=q, fields="files(id,name,mimeType)").execute()
      #    for f in res.get("files", []):
      #        print(f["name"], f["mimeType"], f["id"])
      #    PY

      - name: Run sync script
        run: |
          python scripts/sync_gdrive_to_r2_and_generate_md.py

      #- name: Commit and push changes (if any)
      #  run: |
      #    if git status --porcelain | grep -q .; then
      #      git config user.name "github-actions[bot]"
      #      git config user.email "github-actions[bot]@users.noreply.github.com"
      #      git add _cars/
      #      git commit -m "chore: add new car md files"
      #      git push
      #    else
      #      echo "No changes to commit."
      #    fi